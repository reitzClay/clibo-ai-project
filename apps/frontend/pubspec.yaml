name: clibo_ui
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  web_socket_channel: ^3.0.3
  lottie: ^3.1.0
  flutter_overlay_window: ^0.5.0
  flutter_bloc: ^9.1.1
  intl: ^0.20.2
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/lottie/blob.json # Explicit file path
